$(document).keydown(function(event) {
  if (event.keyCode == 27) {
      // $('#modal01').hide();
      $('.w3-modal').hide();
      // $('#mySidebar').hide();
      $('#navDemo').hide();
      // $('#shortieModal').hide();

      $("#closeMenu").click();
      

  }


  
});
